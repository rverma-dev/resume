# Tailored Resume

Start from `resume/base_resume.md`.

Every changed or reordered bullet must be backed by repository evidence.

Record the final submitted resume path in `applications/applications.json`.
