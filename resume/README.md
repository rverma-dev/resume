# Resume Sources

`base_resume.md` is the canonical IC resume source used by the recruiting
pipeline and published as `IC Resume` in tracker labels. Generated
company-specific resumes belong under `companies/<company-slug>/` and must not
overwrite this file.
